import { Prose_make as Layout } from "../layouts/ApiLayout.gen.js";
export default Layout;

# API Docs

Reason provides a rich set of modules which can be used out of the box without
any extra dependencies. The modules are categorized by compile target.

Please not that we currently don't provide any module documentation for the native
compilation target.
