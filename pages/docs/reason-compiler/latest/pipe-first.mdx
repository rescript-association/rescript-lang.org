# Pipe-first

BuckleScript has a special `|.` (or `->` for Reason) pipe syntax for dealing with various situations. 

This operator has many uses. All the details about this feature can be found [here](/docs/manual/latest/pipe-first).
