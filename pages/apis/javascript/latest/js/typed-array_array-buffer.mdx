import { Docs } from "layouts/JsDocsLayout.bs.js";
import { make as ApiIntro } from "components/ApiIntro.bs.js";
export default Docs.make;

# TypedArray.ArrayBuffer

<ApiIntro>

Provide bindings to JS typed array ArrayBuffer.

</ApiIntro>

## t

```re sig
type t = Js_typed_array.array_buffer;
```

## make

```re sig
let make: int => t;
```

Takes length. initializes elements to 0.

## byteLength

```re sig
let byteLength: t => int;
```

## slice

```re sig
let slice: (~start: int, ~end_: int, t) => Js_typed_array.array_buffer;
```

## sliceFrom

```re sig
let sliceFrom: (int, t) => Js_typed_array.array_buffer;
```
